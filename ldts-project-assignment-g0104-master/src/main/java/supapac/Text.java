package supapac;
/*
//import
import java.util.EnumSet;
import java.util.Objects;

public class Text {
    private String s;
    private String backgroundColor;
    private String foregroundColor;
    private EnumSet<TEXTMODIFIER> tm;

    public Text(String str,String bgc, String fgc) {
        this.s = str;
        this.backgroundColor = bgc;
        this.foregroundColor = fgc;
        this.tm = EnumSet.noneOf(TEXTMODIFIER.class);

        public Text(String str, String bgc, String fgc, EnumSet<TEXTMODIFIER> tm) {
            this.s = str;
            this.backgroundColor = bgc;
            this.foregroundColor = fgc;
            this.tm = tm;
        }

        //Getters & Setters
        public String getString() {return s;}
        public String getBgc() {return backgroundColor;}
        public String getFgc() {return foregroundColor;}

        @Override
        public boolean equals(Object o) {
            if (this == o) return true;
            if (o == null || getClass() != o.getClass()) return false;
            Text text = (Text) o;
            return Objects.equals(string, text.string) && Objects.equals(backgroundColor, text.backgroundColor) && Objects.equals(foregroundColor, text.foregroundColor) && Objects.equals(textmodifiers, text.textmodifiers);
        }

        @Override
        public boolean equals(Object o) {
            if (this == o) return true;
            if (o == null || getClass() != o.getClass()) return false;
            Text text = (Text) o;
            return Objects.equals(string, text.string) && Objects.equals(backgroundColor, text.backgroundColor) && Objects.equals(foregroundColor, text.foregroundColor) && Objects.equals(textmodifiers, text.textmodifiers);
        }

        public enum TEXTMODIFIER {
            BLINK,
            BOLD,
            BORDERED,
            CIRCLED,
            CROSSED_OUT,
            FRAKTUR,
            ITALIC,
            REVERSE,
            UNDERLINE
        }

    }
}
*/