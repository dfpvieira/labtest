/*
import main.java.supapac.Position;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class ElementTest {
    private main.java.supapac.elements.Element ele;
    private main.java.supapac.elements.Element ele1;

    @Test
    void checkGetPosition() {
        Position p = new Position(5, 5);
        ele = new main.java.supapac.elements.Element(p.getX(), p.getX());
        ele1 = new main.java.supapac.elements.Element(p.getX(), p.getY());
        Assertions.assertEquals(ele, ele1);
    }

}
*/  //Erro por class ser abstrata
